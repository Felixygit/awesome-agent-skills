# R50 is paper-trading only. Live broker keys are intentionally unused.
# Optional: used by `r50 serve --mode paper` for public market data.
# YFINANCE has no key. CoinGecko/Binance public endpoints need none.
R50_MODE=demo
R50_HOST=0.0.0.0
R50_PORT=8000
